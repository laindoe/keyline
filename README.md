# KEYLINE

A chroma-key guided vectorizer — mark your own anchor points and guide
lines by hand, and get clean bezier curves fit exactly where you meant
them, instead of fighting Illustrator's Image Trace node explosion.

Single file, no build step: open `keyline.html` in a browser.

See `CLAUDE.md` for full architecture notes, known issues, and where this
was left off.
